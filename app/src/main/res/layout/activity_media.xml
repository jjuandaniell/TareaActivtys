<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="16dp"
    tools:context=".MediaActivity">

    <ImageView
        android:id="@+id/ivDisplayImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@mipmap/ic_launcher" 
        android:contentDescription="Imagen de muestra"
        android:scaleType="centerCrop"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnChangeImage1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mostrar Icono Android"
        android:layout_marginBottom="8dp"/>

    <Button
        android:id="@+id/btnChangeImage2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mostrar Icono Información"
        android:layout_marginBottom="16dp"/>

    <!-- VideoView y elementos relacionados eliminados -->
    <!-- 
    <VideoView
        android:id="@+id/vvDisplayVideo"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="16dp" />
        
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="(Para VideoView, necesitarás un video en res/raw y permisos si es de internet)"
        android:textSize="12sp"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/btnPlayVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reproducir Video de Ejemplo (si está configurado)"
        android:layout_marginBottom="24dp"/>
    -->

    <Button
        android:id="@+id/btnGoToMainFromMedia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Volver al Menú Principal"
        android:layout_marginTop="16dp" />

</LinearLayout>
